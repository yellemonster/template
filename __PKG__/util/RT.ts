export enum URL {
    BASE = "http://localhost:5000",
    // BASE = "https://www.default.app",
}
//
export enum SX {
    test = "test",
    close = "close",
    connection = "connection",
    disconnect = "disconnect",
    //
    userAlert = "userAlert",
    directMsg = "directMsg",
    update = "update",
    //
}
export enum GET {
    test = "/test",
    index = "/",
}
export enum POST {
    test = "/test",
}
